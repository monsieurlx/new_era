# ── Core ──────────────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0

# ── Interactive Brokers API ───────────────────────────────────────────────────
# ib_async is the maintained fork of ib_insync. Identical API, same import style.
ib_async>=1.0.0

# ── Visualization ─────────────────────────────────────────────────────────────
dash>=2.14.0
plotly>=5.18.0

# ── Development fallback (used if IB not connected) ───────────────────────────
yfinance>=0.2.28

# ── Optional: for enhanced data parsing ──────────────────────────────────────
lxml>=4.9.0          # faster HTML table parsing for wikipedia universe fetch
